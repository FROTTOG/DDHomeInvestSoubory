{
  "name": "ddhomeinvest-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf:types": "wrangler types",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.31.0"
  }
}
